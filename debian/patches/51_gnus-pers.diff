## 51_gnus-pers.diff by Peter S Galbraith <psg@debian.org>

--- a/elisp/gnus-bonus-el/gnus-pers.el	2004-08-16 20:49:16.000000000 -0400
+++ b/elisp/gnus-bonus-el/gnus-pers.el	2004-08-16 20:51:59.000000000 -0400
@@ -474,6 +474,7 @@
 					;Let's do From: first.
 	  (if (and from (not (equal from "")))
 	      (progn
+                (goto-char (point-min)) ;Make sure we're in the header
 		(let ((endpos (search-forward mail-header-separator)))
 		  (goto-char (point-min))
 		  (if (re-search-forward "^From:" endpos t)
